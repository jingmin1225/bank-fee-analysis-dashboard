# Local dev — copy to .env.local and fill in
VITE_API_URL=http://localhost:4000/api/v1

# Production (set in Netlify dashboard → Site settings → Environment variables)
# VITE_API_URL=https://bam-api.onrender.com/api/v1
